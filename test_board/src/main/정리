create table board (
 bno int(10) NOT NULL AUTO_INCREMENT PRIMARY KEY,
 title varchar(20),
 writer varchar(20),
 comment varchar(256),
 regdate date,
 moddate date,
 hit int(10)
)



insert into board (title, writer, comment, regdate, hit) values ("test","test", "", DATE_FORMAT(now(), '%Y-%m-%d %h:%i'), 0);
insert into board (title, writer, comment,regdate, hit) values ("123","123","", DATE_FORMAT(now(), '%Y-%m-%d %h:%i'), 0);
DATE_FORMAT(now(), '%Y-%m-%d %h:%i');